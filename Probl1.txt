n = int(input("enter the readings"))
arr = list(map(int, input().split()))
k = int(input())
sum = 0
for i in range(k):
    sum+= arr[i]
maximum = sum
for i in range(k, n):
    sum= sum- arr[i - k] + arr[i]
    if sum > maximum:
        maximum = sum
print(maximum)